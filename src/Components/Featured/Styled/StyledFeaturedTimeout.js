import styled from 'styled-components';
import { COLORS } from '../../../Constants';

export default styled.div`
  background-color: ${COLORS.darkGrey30};
  bottom: 0;
  display: grid;
  grid-template-columns: 32% 68%;
  height: 50px;
  left: 0;
  position: absolute;
  right: 0;
`;
